services:
  - type: web
    name: lifelink
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn lifelink.app:app --bind 0.0.0.0:$PORT
    healthCheckPath: /
    envVars:
      - key: FLASK_ENV
        value: production
